// each folder within app is a nested route. For it to render each folder needs to have a file called page.js

export default function Products(){
    return (
        <h1>Welcome to Our Products Page</h1>
    )
}